import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Home = () => {
  return (
    <SafeAreaView>
      <Text className="h-full bg-gray-600 text-gray-50">
        Homedddddddddddddddddddd
      </Text>
    </SafeAreaView>
  )
}

export default Home
